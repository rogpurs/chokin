# 給料日前日締め 家計・貯金管理アプリ 仕様概要

## 1. アプリ概要

### 1-1. アプリ名（仮）
給料日前日締め 家計・貯金管理アプリ

### 1-2. コンセプト
本アプリは、給料日を基準に1か月の家計と貯金を管理する Web アプリである。  
一般的な暦月ベースではなく、**給料日から次の給料日前日まで**を1期間として扱い、支出記録・貯金目標・月次レポートを通じて、お金の流れを把握しやすくする。

たとえば、給料日が毎月25日の場合は以下のように扱う。

- 2月期: 1月25日〜2月24日
- 3月期: 2月25日〜3月24日

本アプリは銀行自動連携を前提とせず、**手動入力を中心にしながら、入力しやすさと継続しやすさを重視する**。また、Web Push 通知と LINE 連携を備え、日常利用と月次締めを支援する。

---

## 2. 開発目的

本アプリの目的は、以下の課題を解決することである。

- 家計簿アプリが続かない
- 細かく記録したいが入力が面倒
- 給料日ベースで生活しているため、月初〜月末の管理が合わない
- 貯金目標を立てても途中で意識が薄れる
- 1か月の結果を振り返る仕組みがほしい

そのため、本アプリでは以下を重視する。

- 支出を比較的細かく記録できること
- 入力が短時間で終わること
- 給料日前日に1か月を締めてレポートを残せること
- 貯金目標を独立して管理できること
- 通知と LINE により継続利用を支援できること

---

## 3. 想定ユーザー

- 給料日を基準に生活しているユーザー
- アルバイト・パート・学生など、月ごとの収入を意識しながら生活しているユーザー
- 家計簿を続けたいが、高機能すぎるアプリだと挫折しやすいユーザー
- 支出を細かく記録したいが、入力のしやすさも重視するユーザー
- 貯金目標を見える化して継続したいユーザー

---

## 4. 対象環境・技術構成

### 4-1. サーバー環境
- Raspberry Pi サーバー
- Web サーバー上で動作

### 4-2. 使用技術
- HTML
- CSS
- JavaScript
- PHP
- MySQL

### 4-3. 非採用方針
以下は初期段階では使用しない。

- フレームワーク（Laravel, React など）
- 銀行自動 API 連携
- 外部 BaaS
- 複雑なネイティブアプリ機能

---

## 5. アプリ全体方針

### 5-1. 期間の定義
給料日が毎月25日の場合、1期間は以下のように定義する。

- 開始日: 毎月25日
- 締め日: 翌月24日

例:

- 2月期 = 1月25日〜2月24日
- 3月期 = 2月25日〜3月24日

### 5-2. 運用の流れ
1. 日々の支出を入力する
2. 必要に応じて貯金の記録を入力する
3. 次の給料日前日になったら、その期間の結果を締める
4. レポートを保存し、次月に活かす

### 5-3. 設計思想
- 家計簿というより「給料日基準の記録と振り返り」に強いアプリにする
- 銀行自動連携ではなく、手動入力と軽い操作性を優先する
- 細かい記録はできるが、入力負担はできるだけ下げる
- 通知は継続の支援に使い、乱発しない

---

## 6. 主要機能

### 6-1. ユーザー登録・ログイン機能
ユーザーごとに家計データを保持できるよう、アカウント機能を提供する。

#### 機能内容
- 新規ユーザー登録
- ログイン
- ログアウト
- パスワード変更
- プロフィール編集
- 給料日設定
- 通知設定

#### 想定保持情報
- ユーザー名
- メールアドレス
- パスワード（ハッシュ化保存）
- 給料日（初期値 25 日）
- LINE ユーザーID（最大 1 件）
- Web Push トークン/購読情報（最大 1 端末分）

> パスワードは `password_hash()` と `password_verify()` を用いて安全に保存・検証する前提とする。

### 6-2. 支出記録機能
ユーザーが日々の支出を記録できる機能。

#### 入力項目
- 金額
- 日付
- カテゴリ
- メモ（任意）
- 支払い方法（任意）
- 店名・内容（任意）

#### 操作性方針
- テンキー中心の入力
- 最近使ったカテゴリを優先表示
- よく使う金額を再利用しやすくする
- 支出追加後に続けて連続入力できる
- あとから編集・削除しやすくする

### 6-3. カテゴリ管理機能
支出カテゴリを管理する機能。

#### 例
- 食費
- コンビニ
- カフェ
- 交通
- 日用品
- 趣味
- 交際費
- サブスク
- ガジェット
- 固定費

#### 方針
- 初期カテゴリを自動作成
- 必要に応じてユーザーが追加・編集・並び替えできるようにする

### 6-4. 貯金機能
貯金を支出とは独立した機能として扱う。

#### 機能内容
- 貯金目標の作成
- 貯金記録の追加
- 目標ごとの進捗表示
- 目標金額に対する達成率の表示

#### 貯金目標の入力項目
- 目標名
- 目標金額
- 締切日（任意）
- メモ（任意）

#### 表示内容
- 現在の累計貯金額
- 残り必要額
- 達成率
- 今月追加した金額

### 6-5. 月次締め機能
次の給料日前日に、その期間の記録を1か月分として締める機能。

#### 表示項目
- 期間
- 収入
- 支出合計
- カテゴリ別支出
- 残金
- 今月の貯金額
- 多く使ったカテゴリ
- メモ

#### 方針
- 自動集計を行う
- ユーザーがコメントを残せる
- 締めた結果は履歴として保存する

### 6-6. レポート機能
締め処理後に期間レポートを表示・保存する機能。

#### レポート内容例
- 2月期の支出合計
- 2月期のカテゴリ別支出
- 2月期の貯金総額
- 前月比較
- 目標達成率
- 振り返りコメント

### 6-7. Web Push 通知機能
Web Push 通知によって、締め日や入力忘れなどを通知する。

#### 要件
- 1アカウントにつき 1 端末まで対応
- 同一アカウントで複数端末から通知許可が行われた場合、**最後に通知を許可したトークン（購読情報）を上書き保存**する
- 通知送信時は、そのユーザーの最新購読情報に対してのみ送信する

#### 想定通知例
- 給料日前日です。今月の結果を締めましょう
- 今日まだ支出記録がありません
- 貯金目標の期限が近づいています

#### 実装方針
- Service Worker を利用
- ブラウザから通知許可取得
- 購読情報を PHP 経由で MySQL に保存
- 送信対象はユーザーテーブルまたは専用テーブルの最新購読情報1件のみ

### 6-8. LINE 連携機能
LINE 公式アカウントとユーザーを連携し、通知またはメッセージ送信を行えるようにする。

#### 要件
- 1ユーザーにつき LINE ユーザーID は最大 1 件まで
- 連携済みの LINE ユーザーID を `users` テーブルまたは専用連携テーブルに保存する
- 再連携時は上書きする
- 未連携ユーザーには LINE 送信を行わない

#### 想定用途
- 締め日リマインド
- 貯金目標の進捗案内
- 将来的な AI チャット連携の土台

#### 実装方針
- LINE ログインまたは連携用導線を設置
- 取得した LINE ユーザーID をサーバー側で保存
- メッセージ送信は LINE Messaging API を利用する前提で拡張可能にする

---

## 7. 初回セットアップウィザード

### 7-1. 起動条件
初回起動時は管理人ユーザーしか存在しない想定である。  
また、未セットアップ状態では通常画面ではなく、**自動でセットアップウィザードへ遷移**する。

未セットアップ判定例:
- 設定テーブルが存在しない
- 管理ユーザーが未作成
- `installed` フラグが未設定

### 7-2. セットアップウィザードで入力する内容
- アプリ名
- アプリ説明
- 管理者ユーザー名
- 管理者メールアドレス
- 管理者パスワード
- MySQL ホスト
- MySQL データベース名
- MySQL ユーザー名
- MySQL パスワード
- ベース URL
- Web Push 用設定値（必要に応じて後からでも可）
- LINE 用設定値（必要に応じて後からでも可）

### 7-3. セットアップ時の自動処理
- 入力値の検証
- MySQL 接続確認
- 必要テーブルの自動作成
- 初期設定値の保存
- 管理者ユーザー作成
- 初期カテゴリ作成
- インストール完了フラグ保存

### 7-4. セットアップ完了後の挙動
- 通常ログイン画面へ遷移
- セットアップ画面は再度利用不可にする
- 必要であれば管理画面から設定変更可能とする

---

## 8. 画面構成

### 8-1. 初回セットアップ関連
- セットアップ開始画面
- データベース接続確認画面
- アプリ基本情報入力画面
- 管理者作成画面
- セットアップ完了画面

### 8-2. 認証関連
- ログイン画面
- 新規登録画面
- パスワード変更画面
- プロフィール設定画面

### 8-3. メイン画面
- ホーム画面
- 支出入力画面
- 支出一覧画面
- 支出編集画面
- カテゴリ管理画面
- 貯金目標一覧画面
- 貯金目標作成画面
- 貯金入力画面
- 月次締め画面
- 月次レポート一覧画面
- 月次レポート詳細画面
- 通知設定画面
- LINE 連携画面

---

## 9. ホーム画面の想定表示

ホーム画面では、ユーザーが今の状況を一目で把握できるようにする。

### 表示候補
- 現在の対象期間
- 今月の支出合計
- 今月の貯金額
- 次の給料日まであと何日
- 締め日まであと何日
- 貯金目標の進捗
- 最近の記録
- 締め未実施期間の警告

---

## 10. データベース設計（初期案）

### 10-1. settings
アプリ全体設定を保存するテーブル。

主なカラム例:
- `id`
- `app_name`
- `app_description`
- `base_url`
- `installed`
- `created_at`
- `updated_at`

### 10-2. users
ユーザー情報を保存するテーブル。

主なカラム例:
- `id`
- `username`
- `email`
- `password_hash`
- `role`（admin / user）
- `salary_day`
- `line_user_id`（NULL 可、最大 1 件）
- `push_endpoint`（NULL 可）
- `push_p256dh`（NULL 可）
- `push_auth`（NULL 可）
- `push_updated_at`
- `created_at`
- `updated_at`

> Web Push は 1 アカウント 1 端末までなので、最新の購読情報のみを `users` テーブルに保持してもよい。将来的に履歴を分けたい場合は専用テーブル化する。

### 10-3. categories
カテゴリ情報を保存するテーブル。

主なカラム例:
- `id`
- `user_id`
- `name`
- `sort_order`
- `is_default`
- `created_at`
- `updated_at`

### 10-4. expenses
支出情報を保存するテーブル。

主なカラム例:
- `id`
- `user_id`
- `category_id`
- `amount`
- `expense_date`
- `payment_method`
- `shop_name`
- `memo`
- `created_at`
- `updated_at`

### 10-5. saving_goals
貯金目標を保存するテーブル。

主なカラム例:
- `id`
- `user_id`
- `name`
- `target_amount`
- `deadline`
- `memo`
- `created_at`
- `updated_at`

### 10-6. saving_records
貯金記録を保存するテーブル。

主なカラム例:
- `id`
- `user_id`
- `saving_goal_id`
- `amount`
- `record_date`
- `memo`
- `created_at`
- `updated_at`

### 10-7. periods
給料日基準の期間情報を保存するテーブル。

主なカラム例:
- `id`
- `user_id`
- `label`（例: 2026年2月期）
- `start_date`
- `end_date`
- `status`（open / closed）
- `created_at`
- `updated_at`

### 10-8. monthly_reports
月次締め結果を保存するテーブル。

主なカラム例:
- `id`
- `user_id`
- `period_id`
- `income_amount`
- `expense_total`
- `saving_total`
- `balance_amount`
- `comment`
- `closed_at`
- `created_at`
- `updated_at`

### 10-9. optional_logs（任意）
通知送信や LINE 送信のログを残したい場合の任意テーブル。

---

## 11. Web Push 仕様詳細

### 11-1. 保存方針
- 1ユーザーにつき最新の購読情報1件のみ保存
- 同じユーザーが別端末で通知許可を行った場合、既存値を新しい購読情報で上書き
- 送信対象は常に最後に許可した購読情報

### 11-2. 必要保存項目
- endpoint
- p256dh
- auth
- 更新日時

### 11-3. 送信タイミング例
- 締め日前日朝
- 締め日夜
- 貯金期限接近時

### 11-4. エラーハンドリング
- 無効トークン・期限切れの場合は該当購読情報を削除または NULL 化
- 再許可時に再登録する

---

## 12. LINE 仕様詳細

### 12-1. 保存方針
- 1ユーザーに対して保存する LINE ユーザーID は最大 1 件
- 再連携時は上書き
- 解除時は NULL に戻す

### 12-2. 利用用途
- 締め日リマインド
- 月次結果のお知らせ
- 貯金目標達成に関する案内
- 将来的な AI 会話機能の接続先

### 12-3. 実装上の考え方
- まずは「ユーザー連携」と「送信可能状態の保存」までを仕様化
- Messaging API による送信処理は別モジュールとして切り離して実装可能とする

---

## 13. セキュリティ方針

- パスワードは平文保存しない
- CSRF 対策を行う
- セッション固定対策を行う
- SQL インジェクション対策として PDO のプリペアドステートメントを使用する
- セットアップ完了後はセットアップ画面を無効化する
- 管理画面は管理者権限のみに制限する
- LINE と Push のトークン類は必要最小限のみ保存する

---

## 14. MVP（初期実装範囲）

まず実装すべき範囲は以下とする。

1. 初回セットアップウィザード
2. 管理者作成
3. 一般ユーザー登録・ログイン
4. 給料日設定
5. 支出記録
6. カテゴリ管理
7. 貯金目標作成
8. 貯金記録
9. 期間自動生成
10. 月次締め
11. 月次レポート表示
12. Web Push 購読保存
13. LINE ユーザーID 連携保存

---

## 15. 将来的な拡張案

- LINE AI チャット相談機能
- グラフ表示の強化
- CSV エクスポート
- 月次予算機能
- 複数収入対応
- 複数貯金ポケット対応の強化
- テンプレート入力の学習機能
- 支出自動候補機能

---

## 16. まとめ

本アプリは、**給料日を軸に1か月を管理し、支出記録・貯金目標・月次締め・通知連携を組み合わせた家計管理 Web アプリ**である。  
特に、以下を特徴とする。

- 給料日基準の期間管理
- 細かい記録と入力しやすさの両立
- 貯金目標の独立管理
- Web Push 通知対応
- LINE ユーザー連携対応
- PHP / MySQL / Raspberry Pi で動作可能な構成
- 初回セットアップウィザードによる自動初期構築

本仕様は、今後の画面設計・DB 設計・PHP 実装の土台として利用できる。
